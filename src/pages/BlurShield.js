import React from "react";
export default function BlurShield(){ return (<div><h2>BlurShield</h2><p>The Chrome extension injects a blur overlay on risky pages in real-time. It will blur the page when risky text is typed.</p></div>); }
