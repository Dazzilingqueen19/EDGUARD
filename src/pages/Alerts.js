import React from "react";
export default function Alerts(){ return (<div><h2>Alerts</h2><p>Twilio SMS and Firebase push helpers are available in <code>backend/notify.py</code>. Configure env vars to enable.</p></div>); }
