import React,{useState,useEffect} from "react"; export default function Settings(){ const [safeStart,setSafeStart]=useState(6); const [safeEnd,setSafeEnd]=useState(22); const [sensitivity,setSensitivity]=useState(0.3); useEffect(()=>{ const s=localStorage.getItem('safeStart'); if(s) setSafeStart(Number(s)); },[]); function save(){ localStorage.setItem('safeStart',safeStart); localStorage.setItem('safeEnd',safeEnd); localStorage.setItem('sensitivity',sensitivity); alert('Saved (local demo)'); } return (<div><h2>Settings</h2><div><label>Safe start</label><input type='number' value={safeStart} onChange={e=>setSafeStart(e.target.value)} /></div><div><label>Safe end</label><input type='number' value={safeEnd} onChange={e=>setSafeEnd(e.target.value)} /></div><div><label>Sensitivity</label><input type='range' min='0' max='1' step='0.05' value={sensitivity} onChange={e=>setSensitivity(e.target.value)} /></div><button onClick={save}>Save</button></div>); }
