import React from "react";
export default function Emotion(){ return (<div><h2>Webcam Emotion Recognition</h2><p>Run <code>backend/emotion_detector.py</code> to stream webcam emotion events to the backend in real-time.</p></div>); }
