import React from "react"; export default function Timeline({logs}){ if(!logs||!logs.length) return <div>No events.</div>; return (<div>{logs.map(l=>(<div key={l.id} style={{display:'flex',gap:12,padding:10,borderBottom:'1px solid #eee'}}><div style={{width:150,color:'#666'}}>{new Date(l.ts).toLocaleString()}</div><div><strong>{l.event}</strong><div>{l.data}</div><div style={{color:'#666'}}>Risk: {l.risk_score}</div></div></div>))}</div>); }
